version: "3.9"
services:
  disco:
    build: .
    image: "openzeppelin/disco:latest"
    ports:
      - "5000:5000"
    links:
      - ipfs
    environment:
      - IPFS_URL=http://ipfs:5001
  ipfs:
    image: "ipfs/go-ipfs"
    ports:
      - "5001:5001"
